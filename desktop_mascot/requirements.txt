PyQt5>=5.15.0
PyQtWebEngine>=5.15.0
pyttsx3>=2.90
SpeechRecognition>=3.10.0
pyaudio>=0.2.13
google-genai>=0.8.0

# sqlite3 is Python stdlib — no pip install needed
# websockets stdlib implementation used (struct, hashlib, base64, socket)
